# 初始化报告（阶段 A）— 2026-08-18

## 已完成

1. **素材盘点**：13 个旧课件 PDF（共 494 页，第 1~6 章）+ 30 张风格参考图 → `source_inventory/inventory.md`
2. **Control Visual Bible 初稿**：`theme/visual-bible.md`；落地为 `theme/theme.css`（4:3 @1448×1086、深蓝 #1D3FA0 + 橙 #F5821F、页眉徽章/标题装饰线/结论条/底栏全套版式类）
3. **Course Player 最小框架**：`player/index.html`（←/→ 翻页、Space 分步、F 全屏、R 重置、自动隐藏控制条、课次切换、进度记忆）；Review Mode：`review/index.html`（缩略图总览 + page_id + 复杂度/状态标记 + 单页打开）
4. **组件库 P0 最小实现**：`components/components.js`（ResponsePlot 二阶阶跃响应解析解、ParameterSlider）；`theme/act-base.js`（等比缩放 + 分步通信协议）
5. **image2 管线验证通过**：`assets/image2/generate.py` + `image2_config.json`（gpt-image-2，OpenAI 兼容）；首件 `water-tank-level-control.png`（1536×1024，51 s，无字工程插画，风格达标），提示词记录 `prompts/water-tank-level-control.json`
6. **母版验证课**：`lessons/lesson01-intro/` 4 页（L0×2 / L2×1 / L3×1），施工图 `specs/lesson01-intro.yaml`
7. **本地预览**：`server.js` + `package.json`（`npm run dev`，零依赖静态服务器，转发 --port/--host）

## 页面三层分工验证

| page_id | 层级验证点 |
|---|---|
| lesson01-p01/p02 | L0 纯排版，主题版式复刻模板 |
| lesson01-p03 | 视觉层 image2 无字主体图 + 程序绘制 SVG 框图 + Space 三步揭示 |
| lesson01-p04 | 数学层解析解（欠/临界/过阻尼三分支）+ 交互层滑块实时联动指标 |

## 待办 / 待确认

- [ ] **运行验证未完成**：JS 语法检查与本地服务冒烟测试的命令审批超时未执行；首次打开预览时如有报错请反馈
- [ ] 数学对拍：SecondOrder 指标需与 MATLAB/手算样例核对后写入 `qa/`
- [ ] 公式渲染：数学密集章节（第 2 章起）前接入离线 KaTeX
- [ ] 第 6 章旧课件仅 1 个文件（41 页），是否有后续讲次待教师确认
- [ ] 第 5 章根轨迹 104 页单文件，正式制作前需先拆"讲"

## 母版章节建议

推荐以 **第 1 章 绪论（lesson01，已验证）为母版页面集**冻结主题与播放器，首个正式母版章节选 **第 3 章第 2~3 讲（二阶系统分析）**：旧课件结构清晰（31+18 页）、知识点与 L3 组件（PoleZeroExplorer/ResponsePlot）高度匹配、可复用本课 p04 已验证的数学层。
