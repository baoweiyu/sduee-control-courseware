# 自动控制理论三层 HTML 课件生产系统｜工作说明

**文档性质：** 项目总规范 / 长期执行基线  
**版本：** v1.0  
**状态：** 已确认，进入初始化准备阶段  
**生效日期：** 2026-08-18  
**适用范围：** 自动控制理论课程全部章节、全部课次及其后续修订

> **⚠ 开工必读：** 每次开始新章节/新批次制作或修改前，必须先通读
> `specs/制作规范与历史问题清单.md`（用户历次反馈与制作教训汇总，2026-08-30 起持续更新），
> 其中列出的问题绝不允许再次出现；完成修改后如有新的教训，须追加到该清单。

---

## 1. 文档目的

本说明固化自动控制理论三层 HTML 课件生产系统的总体目标、技术边界、角色分工、逐页生产流程、质量门槛和版本管理规则。它是后续所有 Agent、组件、课件页面和审查工作的长期执行依据。

项目的核心目标不是把 PowerPoint 简单搬到网页，也不是把每一页都做成复杂程序，而是建立一套适合教师日常持续使用的课件生产系统：以教师讲授为主，在真正需要形象化理解、动态演示或参数探索的知识点上使用交互；同时尽量减少教师在制作阶段的时间投入。

后续如无用户明确变更，本说明中的规则视为默认规则。任何单节课、单个页面或单个 Agent 的局部决定，都不得与本说明冲突。

## 2. 已确认的总原则

### 2.1 唯一交付与运行形态

HTML 是课程的唯一最终承载形式，也是课堂授课的唯一运行时。

- 不再转换或同步生成 PPTX。
- 不再转换或同步生成 PDF。
- 不封装 EXE。
- 数学、交互和视觉三层可以在制作阶段分别生产，但最终必须拼装进同一个 HTML 课件运行时，不能作为三个互不相干的并列成品交付。
- 实现上可以保留 CSS、JavaScript、SVG、图片、数据和组件等内部文件；对教师而言，最终入口必须统一、明确，打开课程播放器即可授课。

### 2.2 三层分工

每一页在制作阶段都要先拆解对象，再决定对象归属。

| 层级 | 主要职责 | 推荐技术 | 不承担的任务 |
|---|---|---|---|
| 数学逻辑层 | 精确计算、公式、模型、曲线、数据、判据与数值关系 | 数学代码、数值计算库、可复现数据生成程序、LaTeX/KaTeX/MathJax | 不依赖生成式图片给出数值结论 |
| 动态交互层 | 动画、拖拽、分步呈现、状态切换、参数联动、交互反馈 | HTML、CSS、SVG、Canvas、JavaScript | 不伪造数学结果，不以装饰性动画替代因果关系 |
| 视觉呈现层 | 工程场景、设备、科学插图、视觉隐喻、章节视觉、氛围与质感 | Image 2 及后续图像处理 | 不生成关键公式、精确坐标、数学曲线、数值刻度或重要正文 |

三层技术原则统一表述为：

> SVG、Canvas 与程序负责数学世界；Image 2 负责视觉想象力；HTML 是最终舞台。

### 2.3 Image 2 的硬性边界

Image 2 可以生成：

- 工业过程、机械、电机、水箱、飞机、无人机、倒立摆、机器人等工程情境；
- 科学概念插图和便于理解的视觉隐喻；
- 章节封面、过场画面、无文字背景、装饰性素材；
- 便于在 HTML 中叠加标注、箭头和交互对象的无字主体图。

Image 2 禁止承担：

- 关键公式和重要正文；
- 精确坐标轴、刻度和数值；
- 根轨迹、Bode 图、Nyquist 曲线、极点零点图等数学图形；
- 任何需要数值准确、可验证或与参数联动的对象；
- 本应由 SVG/Canvas/程序控制的箭头、路径、高亮和状态。

所有精确数学对象必须由程序生成。所有关键文字必须保留为真实 HTML 文本或公式对象，而不是烘焙进图片。

### 2.4 教学定位

- 课程以教师讲授为主，课件首先服务于讲解节奏和课堂可靠性。
- 不强制采用“六步教学单元”或其他固定课堂设计框架；教师保留自己的 45 分钟课堂组织习惯。
- 交互只用于确实需要形象化理解、动态过程或因果探索的部分。
- 优先制作“操作会导致结果发生清晰变化”的因果演示，不为炫技增加动画。
- 页面可以完全静态，也可以是可运行的交互程序；二者在同一播放器中的授课操作必须一致。

## 3. 用户投入最小化原则

### 3.1 一次性初始化时，用户只需提供

- 所有原始、粗糙或旧版课件；
- 课程素材、教材、讲义、图片、数据、参考资料和必要的计算依据；
- 喜欢的漂亮课件模板、页面截图、风格参考或品牌视觉；
- 本项目实际工作区目录；
- 已知的特殊约束，例如授课设备、浏览器、分辨率、是否离线运行等。

用户提供过的信息不重复询问。缺失信息仅在无法从素材中判断且会实质影响结果时再集中确认。

### 3.2 每节课前，用户只需提供

- 本节课预计页数或内容范围；
- 逐页口述：该页讲什么；
- 逐页口述：大致希望怎样呈现；
- 必须保留、必须删除或必须重点讲解的内容。

口述可以粗略，不要求用户预先写成正式脚本、设计稿或技术需求。

### 3.3 AI 自动完成

AI 负责完成：素材定位与引用、拆页、页面分级、旧页处置判断、对象分类、任务调度、数学核验、视觉素材生成、交互实现、三层拼装、局部构建、课堂运行检查、视觉审查、版本管理和交付说明。

用户最后只做两件事：

1. 验收；
2. 指出需要修改的页和局部问题。

## 4. 页面分类体系

### 4.1 页面复杂度 L0–L3

| 等级 | 定义 | 典型内容 | 默认执行角色 |
|---|---|---|---|
| L0 静态内容/排版页 | 无需新视觉主体或程序交互 | 标题、定义、公式、推导、表格、总结、普通图文 | 主 Agent |
| L1 视觉增强页 | 需要高质量工程情境或科学插图，但无需核心动态交互 | 场景引入、设备剖面、概念隐喻、章节视觉 | 主 Agent + Visual Agent |
| L2 教师主导演示页 | 教师按键或点击逐步呈现、播放动画、高亮路径；学生不必自由探索 | 信号流动、方框图分步化简、推导逐步揭示、局部动态强调 | 主 Agent + Interaction Agent；数学敏感时调用 Math Agent |
| L3 真正交互页 | 参数、拖拽或状态变化引发多对象实时联动，形成可操作的因果实验 | 拖动极点观察响应、参数化根轨迹、保角映射、Nyquist 联动 | 主 Agent + Math Agent + Interaction Agent；必要时增加 Visual Agent |

复杂度的目标是控制成本和风险。不得把整节课默认做成 L2/L3，也不得仅因“网页可以动”就提升等级。

### 4.2 旧课件处置标记

每个旧页面在进入施工前，必须标记为以下一种：

- **KEEP：** 内容与视觉基本可用，直接保留或仅做必要适配；
- **POLISH：** 内容结构保留，只优化层级、排版、颜色、图形或语言；
- **REDESIGN：** 核心内容保留，但页面需要重新组织与重做；
- **INTERACTIVE：** 原页知识点适合转为 L2/L3 动态或交互页面。

这套标记用于避免默认整节课全部推倒重做。

## 5. Agent 组织与调用规则

### 5.1 主 Agent：Course Director

主 Agent 是项目唯一总负责人，不另设拼装 Agent。职责包括：

- 理解用户口述和教学意图；
- 建立素材清单和来源关系；
- 生成 Lesson Spec / Page Map；
- 判断 L0–L3 与 KEEP/POLISH/REDESIGN/INTERACTIVE；
- 对页面对象进行三层归属分类；
- 按需调度专业 Subagents；
- 处理各层之间的接口、状态和版式约束；
- 将三层拼装到同一个 HTML 课件；
- 组织数学、交互、视觉、课堂运行和回归 QA；
- 执行页面冻结、局部重建和版本管理；
- 输出供用户验收的 Review Mode。

### 5.2 三个专业 Subagents

**Math Agent**

- 负责数学模型、公式、推导、算法、数值计算、曲线与数据的正确性；
- 给出可复现的计算逻辑和必要测试；
- 审核交互状态与数学输出是否一致；
- 对资料不足的结论明确标记，不编造公式、数据或实验结果。

**Interaction Agent**

- 负责 HTML/CSS/SVG/Canvas/JavaScript 的交互与动画；
- 负责拖拽、分步呈现、联动、重置、默认状态和课堂操作；
- 保证交互行为有明确教学意义，并与数学逻辑层使用同一数据源；
- 负责性能、键盘操作和常见屏幕尺寸下的稳定性。

**Visual Agent**

- 负责 Image 2 视觉素材、课程视觉语言和图片资产一致性；
- 按页面预留文字、公式和交互叠加区域；
- 避免在生成图中出现关键文字、公式、精确坐标和数学曲线；
- 维护提示词、参考图、版本和资产来源记录。

### 5.3 按需调用矩阵

- L0：主 Agent 独立完成；
- L1：主 Agent + Visual Agent；
- L2：主 Agent + Interaction Agent；若涉及精确数学状态，再调用 Math Agent；
- L3：主 Agent + Math Agent + Interaction Agent；只有在需要工程场景或视觉主体时再调用 Visual Agent。

不得为了形式完整而让每一页同时启动三个 Subagents。调用以页面真实需求为准。

## 6. Lesson Spec / Page Map：唯一施工图

每节课正式制作前，主 Agent 必须先生成结构化 Lesson Spec / Page Map。它是该节课三层生产、局部构建、QA 和版本管理的唯一施工图。

每页至少记录：

- 稳定的页面 ID 与页序；
- 页面主题和教师讲授目标；
- 内容来源及可追溯位置；
- 旧页处置标记；
- L0–L3 复杂度；
- 对象清单及三层归属；
- 数学模型、公式、参数和数据依赖；
- 所需组件；
- 所需 Image 2 资产及其构图要求；
- 交互行为、分步顺序、默认状态和重置行为；
- 教师触发方式；
- 页面状态、版本和验收备注。

建议格式可以是 YAML、JSON 或 Markdown；项目初始化后选择一种主格式并固定，不在课次之间随意切换。示例：

```yaml
page_id: lesson03-p12
title: 二阶系统极点与时域响应
source:
  - file: 旧课件第3章.pptx
    location: slide-18
legacy_action: INTERACTIVE
complexity: L3
teaching_goal: 通过拖动共轭极点理解阻尼比和自然频率对响应的影响
objects:
  math:
    - pole_pair
    - step_response
  interaction:
    - draggable_poles
    - linked_response_plot
    - reset_button
  visual: []
components:
  - ComplexPlane
  - ResponsePlot
  - PoleZeroExplorer
triggers:
  - drag
  - Space
status: Draft
```

未进入 Lesson Spec 的新增对象，原则上不得直接进入成品页面；临时调整必须同步回写施工图。

## 7. 统一课程视觉规范：Control Visual Bible / Theme

初始化阶段必须从用户提供的模板、截图和喜欢的风格中提取统一课程视觉规范。Visual Bible 至少定义：

- 中文、英文、数字和公式字体体系；
- 标题、副标题、正文、注释、标签和页码层级；
- 页面栅格、常用版式、边距和留白；
- 主色、辅助色、中性色、警示色和背景色；
- 线宽、箭头、虚实线、边框、阴影和圆角；
- 公式排版、变量强调和推导步骤样式；
- 坐标轴、网格、刻度、图例和曲线样式；
- 极点、零点、轨迹、稳定区域等专业视觉语义；
- 可拖动、悬停、选中、锁定、禁用和高亮状态；
- Image 2 的统一构图、材质、光照、视角和色彩语言；
- 动画速度、缓动、分步出现和转场原则；
- 屏幕比例、最小字号和课堂后排可读性约束。

视觉规范建立后，后续页面优先复用，不允许各课次任意改变风格。确需新增视觉语义时，应先更新 theme，再应用到页面。

## 8. 可复用组件库

组件库用于把高成本交互变成可复用教学能力。初始优先级如下。

### P0：基础与高频组件

- `BlockDiagram`：方框图、求和点、分支点、信号路径与逐步高亮；
- `ComplexPlane`：复平面、极点零点、区域和映射点；
- `ResponsePlot`：阶跃/脉冲/任意响应曲线与关键指标标注；
- `ParameterSlider`：参数滑块、数值输入、范围约束和重置；
- `StepReveal` / `ExplainStep`：教师主导的逐步显示、推导和说明。

### P1：核心控制理论组件

- `PoleZeroExplorer`；
- `RootLocus`；
- `BodeExplorer`；
- `MappingExplorer`；
- `NyquistExplorer`。

### P2：按课程素材决定的扩展组件

- 信号流图、频域对照、稳定性区域、典型环节、状态空间或其他重复出现的专用组件。

组件必须具备统一主题接口、明确输入输出、默认状态、重置能力、键盘可操作性和最小示例。数学敏感组件必须附带已知样例测试。

## 9. 生产流程

### 阶段 A：一次性初始化

1. 读取用户上传的全部原始课件、课程素材、参考资料和风格参考；
2. 建立素材清单，记录文件、章节、页码、类型、可复用性和来源；
3. 分析旧课件的内容结构、重复模式和明显缺口；
4. 提取并提交 Control Visual Bible / Theme；
5. 建立 Course Player 的最小运行框架；
6. 根据真实课程素材确定组件库优先级；
7. 与用户选择首个母版页面或首个母版章节，用于验证视觉与运行方式；
8. 输出初始化报告和下一步执行计划。

第一阶段不得急于批量制作具体章节。先把素材、风格、运行框架、组件优先级和首个母版选择做正确。

### 阶段 B：单节课规划

1. 接收用户对页数、逐页内容和大致呈现方式的口述；
2. 对照素材库自动定位来源，不重复询问已提供信息；
3. 生成 Lesson Spec / Page Map；
4. 完成 L0–L3、旧页处置标记和三层对象分类；
5. 标出少量真正需要用户决策的冲突或缺失项；
6. 在没有实质冲突时直接进入制作。

### 阶段 C：按页生产与三层拼装

1. Math Agent 生成或审核精确数学对象；
2. Interaction Agent 实现需要的动画、状态和联动；
3. Visual Agent 生成规定范围内的视觉资产；
4. 主 Agent 统一版式、状态、数据接口和触发方式；
5. 将三层合成到同一页面和同一 Course Player；
6. 页面级预览与测试通过后再进入整节课。

三项专业工作按需发生，不要求固定串行，也不要求每页全部发生。

### 阶段 D：QA 与用户验收

1. 自动与人工相结合的数学正确性检查；
2. 交互状态、边界条件和重置检查；
3. 视觉一致性、文字可读性和遮挡检查；
4. Course Player 全屏、翻页、Space 分步和跳转检查；
5. Review Mode 生成缩略图总览；
6. 用户按页面 ID 提出局部修改；
7. 只重建指定页面及其确有依赖的组件；
8. 用户认可后将页面标记为 Approved / Frozen。

## 10. 页面级构建、冻结与版本管理

### 10.1 页面状态

建议统一使用：

- `Draft`：正在制作；
- `Review`：可供用户验收；
- `Approved`：内容和呈现已认可；
- `Frozen`：默认禁止自动重建；
- `Reopen`：经明确要求解冻并进入修改。

用户认可的页面默认转为 Approved，并冻结为 Frozen。后续构建不得使已冻结页面发生随机变化。

### 10.2 局部修改原则

- 每页使用稳定 `page_id`；
- 页面资产、数据和组件依赖可追踪；
- 支持按单页或指定页集合构建；
- 修改一页，不得无故重生成其他页；
- 公共组件变更若影响冻结页，必须先报告影响范围并做回归检查；
- Image 2 资产保留版本，不以新图覆盖唯一原图；
- 每次验收修改都记录页面 ID、问题、修改内容和结果。

## 11. Course Player

Course Player 是教师唯一需要操作的课堂界面，体验应像 PowerPoint 一样简单。

最低要求：

- 一键进入全屏；
- `←` / `→` 上一页、下一页；
- `Space` 推进本页分步或进入下一步；
- `Esc` 退出全屏或关闭临时层；
- 必要时支持数字跳页、目录跳转或页码定位；
- 页面状态可重置；
- 静态页和交互页使用一致的导航逻辑；
- 无网络或网络不稳定时，核心授课功能仍应按项目约束正常运行；
- 常用授课分辨率下不裁切、不溢出，后排可读。

Course Player 不得把教师暴露在开发界面、文件结构或复杂设置中。

## 12. Review Mode

Review Mode 用于快速验收和精确反馈，至少应提供：

- 全节课缩略图总览；
- 清晰页码和稳定页面 ID；
- 页面复杂度、状态和是否冻结的标记；
- 快速打开单页并运行其交互；
- 便于用户用“第几页 / page_id + 局部问题”的方式提意见；
- 修改后只刷新相关页面的预览。

Review Mode 的目标是让用户不必逐个打开源文件，也不必理解技术结构。

## 13. 建议目录结构

实际目录要在读取用户素材和用户指定工作区后最终确定。建议至少包含：

```text
courseware-root/
├─ theme/                 # Control Visual Bible、tokens、字体与主题样式
├─ components/            # 可复用数学与交互组件
├─ lessons/               # 各课次页面实现
├─ assets/
│  └─ image2/             # Image 2 原图、变体、提示词与来源记录
├─ player/                # Course Player
├─ specs/                 # Lesson Spec / Page Map
├─ build/                 # 可运行构建产物
├─ review/                # Review Mode 与缩略图
├─ source_inventory/      # 素材清单与来源索引
└─ qa/                    # 检查记录、测试与验收记录
```

用户的原始素材必须原样保留。系统生成物与源文件分开存放，不覆盖用户提供的原文件。

## 14. 质量门槛

页面进入 Approved / Frozen 前，必须满足与其复杂度相匹配的检查。

### 14.1 内容与来源

- 页面内容可追溯到用户素材、教材、可靠计算或明确的用户口述；
- 不编造缺失的公式、实验、数据、图像结论或参考文献；
- 资料之间有冲突时标明来源和待决项。

### 14.2 数学

- 公式、符号、单位、参数范围和曲线一致；
- 交互状态与数学输出来自同一模型；
- 根轨迹、Bode、Nyquist、响应与极点零点等由程序产生；
- 典型参数、边界参数和重置状态经过测试。

### 14.3 交互

- 默认状态可讲、操作反馈明确、可恢复；
- 拖拽与滑块不越过无意义边界；
- Space 分步顺序符合教师讲解顺序；
- 动画用于解释因果，不遮挡内容，不制造错误暗示；
- 无交互时页面仍有清晰的信息结构。

### 14.4 视觉与课堂

- 符合 Control Visual Bible；
- 关键文字是真实文本，关键公式是真实公式；
- 无文字溢出、遮挡、对比度不足或字号过小；
- Image 2 素材不包含伪公式、乱码或误导性的精确图形；
- 全屏、翻页、跳页和常用分辨率验证通过。

### 14.5 回归与冻结

- 修改公共组件后检查受影响页面；
- 冻结页的视觉和行为未发生非预期变化；
- Review Mode 与实际 Course Player 对应；
- 构建结果可从当前源文件稳定复现。

## 15. 完成定义

一节课只有同时满足以下条件才视为完成：

1. Lesson Spec / Page Map 完整且与成品一致；
2. 所有页面均在统一 Course Player 中运行；
3. 数学、交互和视觉层按边界正确分工；
4. 适用 QA 已通过；
5. Review Mode 可供用户按页检查；
6. 用户验收通过的页面已标记 Approved / Frozen；
7. 未解决问题、资料缺口和后续建议有明确记录。

## 16. 当前阶段与下一步

当前阶段为：**等待用户上传课程素材与风格参考，随后执行一次性初始化。**

收到素材后，主 Agent 应直接：

1. 完整读取用户随后上传的课程素材、旧课件和风格参考；
2. 建立素材清单和来源索引；
3. 完成风格分析，提出 Control Visual Bible 初稿；
4. 评估 Course Player 最小框架；
5. 根据真实内容调整组件库优先级；
6. 推荐首个母版页面或母版章节；
7. 给出初始化结果和下一步执行计划。

不得重复询问用户已经随素材提供的信息，也不得在素材盘点之前批量开始具体章节制作。

---

## 项目一句话约定

> 让程序保证数学准确，让 HTML/SVG/Canvas/JavaScript 提供真正有教学意义的动态与交互，让 Image 2 提供生动统一的视觉素材，最后全部在一个像 PowerPoint 一样易用的 HTML Course Player 中完成课堂讲授。
